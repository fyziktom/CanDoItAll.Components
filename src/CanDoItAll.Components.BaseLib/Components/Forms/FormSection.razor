@namespace CanDoItAll.Components.BaseLib
@{
    var iconName = ResolveIconName();
}

<Stack Section="true"
       GapScale="LayoutGap.Medium"
       AlignItems="AlignItems.Stretch"
       Class="cda-form-section">
    <Split Breakpoint="LayoutBreakpoint.Md"
           Gap="LayoutGap.Medium"
           AlignItems="AlignItems.Start"
           JustifyContent="JustifyContent.SpaceBetween">
        <Stack GapScale="LayoutGap.Tiny">
            <div class="inline-flex min-w-0 items-center gap-2">
                @if (iconName is not null)
                {
                    <span class="cda-form-section__icon">
                        <span class="rz-icon cda-material-icon material-icons text-base" aria-hidden="true">@iconName</span>
                    </span>
                }
                <h2 class="cda-form-section__title">@Title</h2>
                @if (HasHelp)
                {
                    <HelpPopover RootClass="pf-help-popover inline-flex items-center"
                                 ToggleClass="pf-help-popover__toggle pf-help-popover__toggle--compact"
                                 OpenOnHover="true"
                                 AriaLabel="@ResolvedHelpAriaLabel"
                                 Title="@ResolvedHelpAriaLabel"
                                 Heading="@ResolvedHelpHeading"
                                 Body="@(HelpText!)" />
                }
            </div>
            @if (!string.IsNullOrWhiteSpace(Description))
            {
                <p class="cda-form-section__description">@Description</p>
            }
        </Stack>

        @if (Actions is not null)
        {
            <Cluster>
                @Actions
            </Cluster>
        }
    </Split>

    @ChildContent
</Stack>

@code {
    [Parameter, EditorRequired]
    public string Title { get; set; } = string.Empty;

    [Parameter]
    public string? Description { get; set; }

    [Parameter]
    public string? Icon { get; set; }

    [Parameter]
    public string? HelpText { get; set; }

    [Parameter]
    public string? HelpHeading { get; set; }

    [Parameter]
    public RenderFragment? Actions { get; set; }

    [Parameter]
    public RenderFragment? ChildContent { get; set; }

    private bool HasHelp => !string.IsNullOrWhiteSpace(HelpText);

    private string ResolvedHelpHeading => string.IsNullOrWhiteSpace(HelpHeading) ? Title : HelpHeading;

    private string ResolvedHelpAriaLabel => $"Show help for {Title}";

    private string? ResolveIconName()
    {
        return MaterialIconCatalog.TryResolveIconName(Icon, out var iconName)
            ? iconName
            : null;
    }
}



# Common Requests

`CanDoItAll.Components.Common` is owned from the CanDoItAll.Components repo.

Other repos, including Zyphonote, must not patch this shared library directly from their side. File a request in this folder and implement the shared-library change from CanDoItAll.

Preview, demo, tuning, and fake-data assets do not belong in `Common`. Put those in `CanDoItAll.Components.Sandbox`.

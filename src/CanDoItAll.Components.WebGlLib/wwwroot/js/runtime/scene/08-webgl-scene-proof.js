export function getProofSnapshot(state) {
    const symbolCount = Array.from(state.sceneModel.objects || [])
        .reduce((count, sceneObject) => count + (sceneObject.symbols || []).filter(symbol => symbol?.isVisible !== false).length, 0);
    return {
        sceneId: state.sceneModel.sceneId || "",
        objectCount: state.sceneModel.objects?.length || 0,
        linkCount: state.sceneModel.links?.length || 0,
        objectPositions: buildObjectPositions(state),
        visibleObjectCount: state.diagnostics.visibilityCounts?.visibleObjectCount || 0,
        hiddenObjectCount: state.diagnostics.visibilityCounts?.hiddenObjectCount || 0,
        visibleLinkCount: state.diagnostics.visibilityCounts?.visibleLinkCount || 0,
        hiddenLinkCount: state.diagnostics.visibilityCounts?.hiddenLinkCount || 0,
        symbolCount,
        loadedAssetCount: state.diagnostics.loadedAssetIds.size,
        missingAssetCount: state.diagnostics.missingAssetIds.size,
        fallbackObjectCount: state.diagnostics.fallbackObjectIds.size,
        modelInstanceCount: state.diagnostics.modelInstanceIds.size,
        primitiveInstanceCount: state.diagnostics.primitiveInstanceIds.size,
        estimatedTriangleCount: state.diagnostics.estimatedTriangleCount || 0,
        estimatedVertexCount: state.diagnostics.estimatedVertexCount || 0,
        activeMotionCount: state.motions?.size || 0,
        queuedMotionCount: state.diagnostics.queuedMotionCount || 0,
        activeMotionIds: state.diagnostics.activeMotionIds || [],
        queuedMotionIds: state.diagnostics.queuedMotionIds || [],
        motionQueueSnapshot: state.diagnostics.motionQueueSnapshot || [],
        currentCommandBatchId: state.diagnostics.currentCommandBatchId || "",
        currentCommandStageId: state.diagnostics.currentCommandStageId || "",
        completedCommandStageCount: state.diagnostics.completedCommandStageCount || 0,
        failedCommandStageCount: state.diagnostics.failedCommandStageCount || 0,
        skippedCommandStageCount: state.diagnostics.skippedCommandStageCount || 0,
        queuedCommandStageCount: state.diagnostics.queuedCommandStageCount || 0,
        commandStageBarrierPolicy: state.diagnostics.commandStageBarrierPolicy || "",
        commandStageWaitSeconds: state.diagnostics.commandStageWaitSeconds || 0,
        commandStageBarrierTarget: state.diagnostics.commandStageBarrierTarget || "",
        commandStageBarrierBlockers: state.diagnostics.commandStageBarrierBlockers || [],
        commandStageBarrierEventId: state.diagnostics.commandStageBarrierEventId || "",
        commandStageResultLog: state.diagnostics.commandStageResultLog || [],
        commandStageQueueSnapshot: state.diagnostics.commandStageQueueSnapshot || [],
        commandStageJournalCount: state.diagnostics.commandStageJournalCount || 0,
        commandStageJournalDroppedCount: state.diagnostics.commandStageJournalDroppedCount || 0,
        commandStageJournalCounters: state.diagnostics.commandStageJournalCounters || {},
        commandStageRecentResultIds: state.diagnostics.commandStageRecentResultIds || [],
        commandStageRecentJournalEntries: state.diagnostics.commandStageRecentJournalEntries || [],
        runtimeStopCount: state.diagnostics.runtimeStopCount || 0,
        lastRuntimeStopReason: state.diagnostics.lastRuntimeStopReason || "",
        clearedMotionCount: state.diagnostics.clearedMotionCount || 0,
        lastRuntimeStopClearedMotionCount: state.diagnostics.lastRuntimeStopClearedMotionCount || 0,
        lastRuntimeStopCancelledCommandStageCount: state.diagnostics.lastRuntimeStopCancelledCommandStageCount || 0,
        renderCount: state.diagnostics.renderCount || 0,
        isRenderLoopActive: !!state.diagnostics.isRenderLoopActive,
        averageFrameTimeMs: Math.round((state.diagnostics.averageFrameTimeMs || 0) * 100) / 100,
        peakFrameTimeMs: Math.round((state.diagnostics.peakFrameTimeMs || 0) * 100) / 100,
        fullSceneRebuildCount: state.diagnostics.fullSceneRebuildCount || 0,
        transformOnlyPatchCount: state.diagnostics.transformOnlyPatchCount || 0,
        symbolOnlyPatchCount: state.diagnostics.symbolOnlyPatchCount || 0,
        linkOnlyPatchCount: state.diagnostics.linkOnlyPatchCount || 0,
        visualReplacePatchCount: state.diagnostics.visualReplacePatchCount || 0,
        mixedIncrementalPatchCount: state.diagnostics.mixedIncrementalPatchCount || 0,
        graphStructurePatchCount: state.diagnostics.graphStructurePatchCount || 0,
        sceneRebuildPatchCount: state.diagnostics.sceneRebuildPatchCount || 0,
        lastPatchClassification: state.diagnostics.lastPatchClassification || "",
        linkUpdateCount: state.diagnostics.linkUpdateCount || 0,
        linkGeometryUpdateCount: state.diagnostics.linkGeometryUpdateCount || 0,
        linkGeometryRebuildCount: state.diagnostics.linkGeometryRebuildCount || 0,
        linksUpdatedLastFrame: state.diagnostics.linksUpdatedLastFrame || 0,
        linkSyncScanCount: state.diagnostics.linkSyncScanCount || 0,
        linkSyncIndexedHitCount: state.diagnostics.linkSyncIndexedHitCount || 0,
        activeAssetProfile: state.sceneModel.uiState?.activeAssetProfile || state.options.assetQualityProfile || "primitive",
        largestLoadedAssetId: state.diagnostics.largestLoadedAssetId || "",
        assetCacheMode: state.diagnostics.assetCacheMode || state.assetCache?.mode || "state-local",
        assetCacheEntryCount: state.diagnostics.assetCacheEntryCount || 0,
        assetCacheHitCount: state.diagnostics.assetCacheHitCount || 0,
        assetCacheMissCount: state.diagnostics.assetCacheMissCount || 0,
        disposedTemplateCount: state.diagnostics.disposedTemplateCount || 0,
        assetCachePendingDisposalCount: state.diagnostics.assetCachePendingDisposalCount || 0,
        assetCacheDisposedPromiseCount: state.diagnostics.assetCacheDisposedPromiseCount || 0,
        assetCacheDisposalErrorCount: state.diagnostics.assetCacheDisposalErrorCount || 0,
        materialCloneCount: state.diagnostics.materialCloneCount || 0,
        disposedGeometryCount: state.diagnostics.disposedGeometryCount || 0,
        disposedMaterialCount: state.diagnostics.disposedMaterialCount || 0,
        disposedTextureCount: state.diagnostics.disposedTextureCount || 0,
        retainedSharedTextureCount: state.diagnostics.retainedSharedTextureCount || 0,
        selectedObjectIds: Array.from(state.selectedObjectIds || []),
        hoveredObjectId: state.hoveredObjectId || "",
        viewportWidth: state.viewport.width,
        viewportHeight: state.viewport.height,
        metadata: {
            runtime: "webglScene",
            deterministicMode: String(state.options.deterministicMode),
            renderMode: state.options.renderMode || "auto",
            lastFrameReason: state.diagnostics.lastFrameReason || "",
            lastScheduledReason: state.diagnostics.lastScheduledReason || "",
            runtimeStopCount: String(state.diagnostics.runtimeStopCount || 0),
            lastRuntimeStopReason: state.diagnostics.lastRuntimeStopReason || "",
            clearedMotionCount: String(state.diagnostics.clearedMotionCount || 0),
            lastRuntimeStopClearedMotionCount: String(state.diagnostics.lastRuntimeStopClearedMotionCount || 0),
            lastRuntimeStopCancelledCommandStageCount: String(state.diagnostics.lastRuntimeStopCancelledCommandStageCount || 0),
            lastDeltaSeconds: String(state.diagnostics.lastDeltaSeconds || 0),
            frameTimeMs: String(Math.round((state.diagnostics.frameTimeMs || 0) * 100) / 100),
            averageFrameTimeMs: String(Math.round((state.diagnostics.averageFrameTimeMs || 0) * 100) / 100),
            peakFrameTimeMs: String(Math.round((state.diagnostics.peakFrameTimeMs || 0) * 100) / 100),
            isRenderLoopActive: String(!!state.diagnostics.isRenderLoopActive),
            assetCacheMode: state.diagnostics.assetCacheMode || state.assetCache?.mode || "state-local",
            assetCacheEntryCount: String(state.diagnostics.assetCacheEntryCount || 0),
            assetCacheHitCount: String(state.diagnostics.assetCacheHitCount || 0),
            assetCacheMissCount: String(state.diagnostics.assetCacheMissCount || 0),
            disposedTemplateCount: String(state.diagnostics.disposedTemplateCount || 0),
            assetCachePendingDisposalCount: String(state.diagnostics.assetCachePendingDisposalCount || 0),
            assetCacheDisposedPromiseCount: String(state.diagnostics.assetCacheDisposedPromiseCount || 0),
            assetCacheDisposalErrorCount: String(state.diagnostics.assetCacheDisposalErrorCount || 0),
            materialCloneCount: String(state.diagnostics.materialCloneCount || 0),
            disposedGeometryCount: String(state.diagnostics.disposedGeometryCount || 0),
            disposedMaterialCount: String(state.diagnostics.disposedMaterialCount || 0),
            disposedTextureCount: String(state.diagnostics.disposedTextureCount || 0),
            retainedSharedTextureCount: String(state.diagnostics.retainedSharedTextureCount || 0),
            patchedObjectCount: String(state.diagnostics.patchedObjectCount || 0),
            fullSceneRebuildCount: String(state.diagnostics.fullSceneRebuildCount || 0),
            transformOnlyPatchCount: String(state.diagnostics.transformOnlyPatchCount || 0),
            symbolOnlyPatchCount: String(state.diagnostics.symbolOnlyPatchCount || 0),
            linkOnlyPatchCount: String(state.diagnostics.linkOnlyPatchCount || 0),
            visualReplacePatchCount: String(state.diagnostics.visualReplacePatchCount || 0),
            mixedIncrementalPatchCount: String(state.diagnostics.mixedIncrementalPatchCount || 0),
            graphStructurePatchCount: String(state.diagnostics.graphStructurePatchCount || 0),
            sceneRebuildPatchCount: String(state.diagnostics.sceneRebuildPatchCount || 0),
            lastPatchClassification: state.diagnostics.lastPatchClassification || "",
            replacedObjectGroupCount: String(state.diagnostics.replacedObjectGroupCount || 0),
            symbolOnlyUpdateCount: String(state.diagnostics.symbolOnlyUpdateCount || 0),
            linkUpdateCount: String(state.diagnostics.linkUpdateCount || 0),
            linkGeometryUpdateCount: String(state.diagnostics.linkGeometryUpdateCount || 0),
            linkGeometryRebuildCount: String(state.diagnostics.linkGeometryRebuildCount || 0),
            linksUpdatedLastFrame: String(state.diagnostics.linksUpdatedLastFrame || 0),
            linkSyncScanCount: String(state.diagnostics.linkSyncScanCount || 0),
            linkSyncIndexedHitCount: String(state.diagnostics.linkSyncIndexedHitCount || 0),
            sceneIndexSyncCount: String(state.diagnostics.sceneIndexSyncCount || 0),
            lastSceneIndexSyncReason: state.diagnostics.lastSceneIndexSyncReason || "",
            modelDiagnosticsCount: String(state.diagnostics.modelDiagnostics?.size || 0)
        }
    };
}

function buildObjectPositions(state) {
    const positions = {};
    for (const [objectId, position] of state.objectPositions || []) {
        if (!objectId || !position) {
            continue;
        }

        positions[objectId] = {
            x: roundNumber(position.x),
            y: roundNumber(position.y),
            z: roundNumber(position.z)
        };
    }

    return positions;
}

function roundNumber(value) {
    return Math.round((Number(value) || 0) * 1000) / 1000;
}

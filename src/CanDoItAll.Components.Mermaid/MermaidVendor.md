# Mermaid Vendor Asset

The files in `wwwroot/js/vendor` were downloaded from the official Mermaid package on jsDelivr.

- Mermaid package version: `11.14.0`
- Root CDN URL: `https://cdn.jsdelivr.net/npm/mermaid@11.14.0/dist/mermaid.esm.min.mjs`
- Downloaded for: `CanDoItAll.Components.Mermaid`
- Rule: do not edit these vendor files by hand and do not build Mermaid from source for this package.

The top-level ESM module imports chunk files, so the referenced `dist/chunks/mermaid.esm.min/*.mjs` files are included as package resources as well.

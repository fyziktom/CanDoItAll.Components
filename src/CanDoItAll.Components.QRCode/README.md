# CanDoItAll.Components.QRCode

Shared Blazor QR generation and scanner shell components.

The project owns generic QR UI only: payload display, SVG rendering, copy actions, scanner shell, and manual decoded-text fallback. Ledger-specific address, transaction, business-object, and ticket payload semantics belong in consuming applications or domain component libraries.

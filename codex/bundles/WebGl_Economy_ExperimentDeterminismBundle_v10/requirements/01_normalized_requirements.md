# Normalized requirements

1. Audit current implementation in both repositories.
2. Find what Codex skipped, simplified, or overfit.
3. Use shared-well as a readiness probe, but do not implement final demo.
4. Use another example, farmer-land, to avoid overfitting.
5. Apply Vernon L. Smith-inspired experimental discipline: explicit, controlled, reproducible inputs.
6. Ensure randomization is pre-run generation into versioned JSON, not hidden runtime randomness.
7. Ensure scenario inputs, placement, parameters, rules, run plans and visual mappings are hashable.
8. Ensure simulation and visualization cores stay generic and flexible.
9. Keep WebGL large-screen-only.
10. Provide a detailed follow-up bundle with XLSX execution matrix.

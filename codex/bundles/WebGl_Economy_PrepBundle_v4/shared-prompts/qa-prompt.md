# QA Prompt

- Verify branch/status, builds, tests, dependency scans, WebGL audits, and browser screenshots.
- Fail closure if WebGL scene/model screenshots are blank, if cross-repo coupling appears, or if deterministic hash tests are missing.
- Check raw notes literally against shipped code and proof.


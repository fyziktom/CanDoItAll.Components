# Shared finite resource probe

Do not create a final UI demo yet.

The real scenario runner must prove:

- input pack loads in strict mode,
- backend emits frames/deltas,
- visual mapper emits movement/admin/symbol actions,
- bridge emits run document with initial scene and executable stages,
- snapshot pipeline emits snapshots for all frames,
- analysis can identify admin burden, issues, resource concentration and pending visual stages.

Expected output artifact:

```text
artifacts/economy/real-scenario-runs/shared-well/readiness-report.json
```

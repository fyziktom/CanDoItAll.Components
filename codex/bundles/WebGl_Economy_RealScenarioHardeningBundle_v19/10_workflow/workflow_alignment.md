# Workflow alignment

Codex must follow the CanDoItAll bundle workflow style:

- inventory first,
- implement subbundles in order,
- maintain proof manifests,
- record changed files,
- add semantic invariants,
- run validation commands,
- write an execution report,
- do a red-team closure review.

Empty transcript files are not acceptable as proof.

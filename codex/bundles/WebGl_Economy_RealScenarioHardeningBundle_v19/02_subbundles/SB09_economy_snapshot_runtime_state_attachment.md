# SB09 - Economy snapshot runtime state attachment

Codex must harden snapshot visual/runtime attachment.

Add fields or metadata for:

- current playback command,
- active motion ids,
- queued motion ids,
- active barrier policy,
- active barrier blockers,
- command journal tail,
- render diagnostics summary,
- selected object/action ids if supplied by UI.

The deterministic snapshot hash should include data-state and optionally visual-runtime state as separate hashes.

## Status

Completed.

## Goal

Attach visual runtime state to Economy snapshots without contaminating deterministic data-state hashing.

## Prerequisites

- SB08 runner artifacts must exist or be generated by tests.

## Owned Requirements

- R09 Snapshot Runtime Attachment.

## Dependency Impact

Feeds SB10 snapshot analysis and SB12 readiness reporting for missing browser playback fields.

## Validation Depth

Critical data contract proof with serialization, hashing, positive runtime attachment, negative hash separation, and source assertions.

## Proof Required

- Economy tests for runtime attachment serialization and separate hashes.
- Source assertions for all required runtime fields.
- Proof manifest and semantic invariant contract.

Proof captured in `bundle://proof/SB09/manifest.md`.

## Progression Gate

Pass only when deterministic data-state hash is stable without runtime state and optional visual-runtime hash changes when runtime diagnostics change.

Gate result: Passed. Snapshot runtime attachments now serialize current playback command, motion queues, barrier state, command journal tail, render diagnostics, and selected ids; `snapshot.dataState` remains stable while `snapshot.visualRuntime` changes with runtime diagnostics.

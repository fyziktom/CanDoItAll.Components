# SB01 - Components inventory and regression gate

Repository: `CanDoItAll.Components`

## Goal

Create a fresh inventory for the current `webgl-engine` branch and make sure the next work starts from the existing implementation.

## Required checks

- Confirm current branch and do not create a new one.
- List all `WebGlLib` JS runtime files and line counts.
- List all `WebGlRunLib` files and summarize responsibilities.
- Confirm `WebGlWorkbench` namespace still exists.
- Confirm `WebGlScene` namespace still exists.
- Confirm `WebGlRunLib` has no economy references.

## Evidence

Save under:

```text
artifacts/webgl-economy-followup-v5/components/SB01/
```

Include:
- `branch.txt`
- `runtime-line-counts.txt`
- `webglrunlib-inventory.md`
- `regression-summary.md`

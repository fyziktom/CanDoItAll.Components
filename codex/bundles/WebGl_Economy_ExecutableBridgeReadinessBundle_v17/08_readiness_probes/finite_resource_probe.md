# Finite-resource readiness probe

Do not build final UI demo in this bundle.

Use finite resource / constrained expansion only to verify generic capability:

- finite resource capacity;
- ownership-like resource stores;
- transfer/trade events;
- concentration metrics;
- rule/invariant enforcement;
- visual relationship/resource transfer and admin/rule symbols.

Pass condition:

- no hardcoded finite-resource example names outside fixtures/scenario factories;
- metrics/invariants run against generic resource IDs;
- bridge projection remains independent of specific resource semantics.

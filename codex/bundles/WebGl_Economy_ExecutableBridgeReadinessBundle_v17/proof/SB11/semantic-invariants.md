# SB11 Semantic Invariants

| Invariant ID | Shallow-pass trap | Adversarial negative proof | Semantic positive proof | Anti-stub audit |
| --- | --- | --- | --- | --- |
| SB11-INV-01 | A performance proof could report elapsed time without proving required scale. | Test assertions require exact threshold counts: 100 actors, 20 resources, 500 events, and 1000 visual actions. | `simulation-performance-proof.json` stores thresholds and actuals. | The artifact is generated by the passing test, then copied into bundle proof. |
| SB11-INV-02 | Counting actions alone could miss command queue pressure. | The command-batch proof records stage count, motion count, document size, and max per-object queue length. | `WebGlRunFrameApplyResult.FromFrame` normalizes the real stress run frame. | Assertions require nonzero snapshot JSON size and at least 500 commands/motions. |

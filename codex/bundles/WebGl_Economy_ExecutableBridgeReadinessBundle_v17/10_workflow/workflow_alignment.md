# Workflow alignment

This bundle follows the CanDoItAll bundle workflow pattern:

- source observations are mapped to subbundles;
- critical subbundles require proof;
- implementation should be phase-gated;
- closure must be artifact-backed;
- unresolved gaps must become follow-up subbundles, not vague residual risk.

Codex must not skip validation because the code builds locally once.

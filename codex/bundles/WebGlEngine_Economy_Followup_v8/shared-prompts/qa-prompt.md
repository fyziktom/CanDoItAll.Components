# QA prompt

Act as a senior QA inspector. For each subbundle, verify:

- failing-first proof exists or an exception is justified,
- changed code is not a stub,
- warnings cannot masquerade as research-ready,
- browser observer evidence does not mutate headless truth,
- factor/oracle/metric paths cannot silently default,
- proof artifacts are non-empty and include assertions.

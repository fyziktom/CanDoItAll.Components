# QA prompt

Review the completed subbundle as a skeptical senior QA inspector. Reject completion if proof files are empty, if browser proof lacks assertions, if generic package boundaries leak domain semantics, if replay semantics are ambiguous, or if tests only prove compilation.

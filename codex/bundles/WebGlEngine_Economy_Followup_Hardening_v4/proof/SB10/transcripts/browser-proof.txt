SB10 browser proof transcript

Setup command:
dotnet run --project src\CanDoItAll.Components.WebGlSandbox\CanDoItAll.Components.WebGlSandbox.csproj --no-restore --urls http://127.0.0.1:5268

Result:
Sandbox served http://127.0.0.1:5268/run-playback with HTTP 200. The temporary sandbox process was stopped after proof capture.

Browser route:
http://127.0.0.1:5268/run-playback

Browser actions:
1. Opened the Run Playback page in the in-app browser.
2. Confirmed initial visible state showed frame 0, status `Ready.`, and runtime text `26 objects / 0 symbols / 0 loaded / 0 fallback`.
3. Clicked `Step`.
4. Confirmed diagnostics after Step:
   - `currentFrameIndex`: 1
   - `status`: `Applied generic frame 1.`
   - `latestApply.success`: true
   - `latestApply.frameIndex`: 1
   - `latestApply.appliedStageCount`: 2
   - `batch.currentCommandBatchId`: `run-frame:1`
   - `proofSnapshot.objectCount`: 26
   - `proofSnapshot.visibleObjectCount`: 26
5. Clicked `Snapshot` to force a component re-render/state refresh.
6. Confirmed diagnostics after Snapshot:
   - `currentFrameIndex`: 1
   - `latestApply.success`: true
   - `runSnapshot.currentCommandBatchId`: `run-frame:1`
   - `proofSnapshot.objectCount`: 26
   - `proofSnapshot.visibleObjectCount`: 26

Screenshot artifact:
`browser/run-playback-after-import-step-rerender.png`

Semantic assertion:
The real browser path imported the WebGlRun initial scene document through `WebGlSceneViewBrowserRuntime`, applied frame 1, triggered an additional re-render via Snapshot, and preserved the applied/imported runtime scene: frame state stayed at 1 and 26 objects remained visible after the re-render.

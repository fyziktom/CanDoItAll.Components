# QA prompt

Review the completed subbundle as a senior QA inspector. Verify that proof is non-empty, machine-readable where required, and demonstrates the semantic behavior rather than just build success. Reopen if the proof could be a false positive.

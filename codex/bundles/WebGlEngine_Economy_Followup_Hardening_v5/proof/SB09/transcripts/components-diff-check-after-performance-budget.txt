Full `git diff --check` emitted existing LF-to-CRLF working-copy warnings across prior bundle files and earlier subbundle edits, which caused a non-zero PowerShell-wrapped command result without identifying SB09 whitespace errors.
SB09-specific diff check passed; see `components-sb09-diff-check.txt`.

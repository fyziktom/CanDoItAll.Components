# SB01 - Cross-repo inventory and branch guard

Run first in both repos.

Required:

- capture current branch, status, latest commit;
- verify no new branch is created;
- list changed files since previous baseline;
- identify whether Components work is on `webgl-engine` and Economy work is on the currently checked-out branch;
- record the local paths of both repos.

Evidence:

- `artifacts/scenario-followup/branch-inventory-components.txt`
- `artifacts/scenario-followup/branch-inventory-economy.txt`

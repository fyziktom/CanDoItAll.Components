Command: node codex\bundles\CanvasFloatingWindows_PublishingReadiness_v1\proof\SB04\verify-canvas-smoke.cjs
ExitCode: 1

Result: FAIL
Failure 1: max-desktop calendar state assertion used a private WeakMap implementation detail and was corrected to assert public facade plus visible DOM.
Failure 2: desktop smoke exposed the real layout defect after the assertion correction:
page.waitForSelector: Timeout 30000ms exceeded while waiting for `.zy-calendar-shell, .zy-calendar-canvas` to be visible.

Observed diagnostic after failure:

- `.cdi-canvas-calendar-shell` width: 2px.
- `.zy-calendar-shell` width: 0px.
- `.zy-calendar-canvas` width: 2px.

Root cause:

- `SectionCard` renders as a vertical flex stack with `align-items:flex-start`.
- `CanvasCalendar` root shell did not declare full width, so its flex item shrink-wrapped to a near-zero intrinsic width in the sandbox proof layout.

Fix proven later:

- `repo://src/CanDoItAll.Components.CanvasLib/wwwroot/css/workbench/panels/03-help-settings-and-preview.css` now sets `.cdi-canvas-calendar-shell` to `width: 100%` and `min-width: 0`.
- Final passing transcript: `bundle://proof/SB04/transcripts/playwright-canvas-smoke.txt`.

@namespace CanDoItAll.Components.Sandbox.Components.Examples.Lists

<ListGroup data-testid="sandbox-lists-list-group">
    <ListItem>
        <TextBlock TextStyle="@(TextStyle.Subtitle2)" Value="Layout shell" />
        <TextBlock TextStyle="@(TextStyle.Body2)"
                   Value="Shared shell keeps main content, rail, and sticky actions in distinct regions." />
    </ListItem>
    <ListItem>
        <TextBlock TextStyle="@(TextStyle.Subtitle2)" Value="Detail pane" />
        <TextBlock TextStyle="@(TextStyle.Body2)" Value="The selected record has enough room for operational detail." />
    </ListItem>
    <ListItem>
        <TextBlock TextStyle="@(TextStyle.Subtitle2)" Value="Dense review lane" />
        <TextBlock TextStyle="@(TextStyle.Body2)" Value="Denser sections still need obvious grouping." />
    </ListItem>
</ListGroup>

@namespace CanDoItAll.Components.Sandbox.Components.Examples.Canvas

<CalendarMiniMonthNavigator Snapshot="@Snapshot" />

@code {
    private CanvasCalendarSurface Surface => SandboxCanvasSamples.CreateCalendarSurface();

    private CalendarMiniMonthNavigatorSnapshot Snapshot => CalendarMiniMonthNavigatorFactory.Create(Surface);
}

@namespace CanDoItAll.Components.Sandbox.Components.Examples.Inputs

<FormField Text="Status">
    <DropDown TValue="string"
              Data="@StatusOptions"
              TextProperty="@nameof(SelectOption.Text)"
              ValueProperty="@nameof(SelectOption.Value)"
              Value="@status"
              ValueChanged="@(value => status = value ?? StatusOptions[0].Value)"
              Placeholder="Select status" />
</FormField>

@code {
    private sealed record SelectOption(string Text, string Value);

    private static readonly IReadOnlyList<SelectOption> StatusOptions =
    [
        new("Draft", "Draft"),
        new("Review", "Review"),
        new("Blocked", "Blocked"),
        new("Approved", "Approved")
    ];

    private string status = StatusOptions[0].Value;
}

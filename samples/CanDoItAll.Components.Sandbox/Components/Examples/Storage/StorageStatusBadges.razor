@namespace CanDoItAll.Components.Sandbox.Components.Examples.Storage

<StorageBadgeStrip Badges="@(new List<StorageSummaryBadge>
                  {
                      new("Plan: Pro", "info"),
                      new("82% used", "warning"),
                      new("Backups on", "success")
                  })" />

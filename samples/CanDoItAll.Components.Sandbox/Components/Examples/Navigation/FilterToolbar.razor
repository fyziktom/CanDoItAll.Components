@namespace CanDoItAll.Components.Sandbox.Components.Examples.Navigation
@inherits NavigationExampleBase

<Toolbar data-testid="sandbox-navigation-toolbar">
    <ToolbarRow>
        <ToolbarFields>
            <TextInput Value="@toolbarQuery"
                       ValueChanged="HandleToolbarQueryChanged"
                       ValueExpression="@(() => toolbarQuery)"
                       Immediate="true"
                       placeholder="Filter standard navigation"
                       aria-label="Filter standard navigation"
                       data-testid="sandbox-navigation-toolbar-query" />
            <SelectInput TValue="string"
                         Value="@toolbarScope"
                         ValueChanged="HandleToolbarScopeChanged"
                         ValueExpression="@(() => toolbarScope)"
                         aria-label="Navigation scope"
                         data-testid="sandbox-navigation-toolbar-scope">
                @foreach (var scope in ToolbarScopes)
                {
                    <option value="@scope">@scope</option>
                }
            </SelectInput>
        </ToolbarFields>
        <ToolbarActions>
            <Button Text="Reset"
                    Tone="@(ButtonTone.Default)"
                    Variant="@(Variant.Outlined)"
                    Click="@ResetNavigationTools"
                    data-testid="sandbox-navigation-toolbar-reset" />
        </ToolbarActions>
    </ToolbarRow>
</Toolbar>
